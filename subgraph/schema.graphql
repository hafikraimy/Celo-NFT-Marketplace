type ListingEntity @entity {
  id: ID!
  nftAddress: Bytes! # address
  tokenId: BigInt! # uint256
  price: BigInt! #uint256
  seller: Bytes! # address
  # buyer is an optional property 
  # listing will not have a buyer until it is sold
  buyer: Bytes #address
}
